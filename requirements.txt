# requirements.txt — pending / temporal RAG (SurrealDB + Gemini)
# Pin after `uv pip install -U -r requirements.txt` + tests: uv pip freeze > requirements-lock.txt

# Database
surrealdb>=1.0.8

# API & environment
python-dotenv>=1.0.0
aiohttp>=3.9.0
openai>=1.50.0

# Data
pydantic>=2.10.0

# Google Gemini (official SDK; replaces deprecated google-generativeai)
google-genai>=1.0.0
genai-prices>=0.0.40

# ML / embeddings (CPU wheels; install torch separately if needed)
transformers>=4.45.0
sentence-transformers>=3.0.0
numpy>=2.0.0
scipy>=1.14.0
scikit-learn>=1.5.0

# Utilities
tqdm>=4.66.0
pillow>=10.0.0

# PyTorch (optional GPU): see https://pytorch.org/get-started/locally/
