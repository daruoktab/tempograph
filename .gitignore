# Python
__pycache__/
*.pyc

# Local-only: agent skills (Cursor / tooling) — do not push
.agents/
skills-lock.json

# Local-only: repo / branding name notes (not for remote)
REPO_NAMING.local.md

# Local ty checker output (do not commit)
ty_errors.txt
ty_remaining.txt

# Environment
.env

# Local Conda spec (use committed environment.example.yml as template)
environment.yml

# Token usage logs (runtime generated); keep shipped example for reference
output/**/token_usage.jsonl
!/output/example_dataset/token_usage.jsonl

# JetBrains IDE settings
.idea/
data/chroma_test/
*.log
